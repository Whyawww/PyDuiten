import { HeroSection } from '../../../component/homepage/HeroSection';

export const Home = () => {
    return (
        <div className="flex flex-col min-h-screen">
            <HeroSection />
        </div>
    );
};